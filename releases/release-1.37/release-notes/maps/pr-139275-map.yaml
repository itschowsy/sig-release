pr: 139275
pr_body: ''
releasenote:
  text: Fixed a `kubelet` issue where Pods with `subPath` mounts could become stuck
    in an error loop after FUSE or GlusterFS network filesystem disruptions.
  sigs:
  - node
  - storage
  - testing
  kinds:
  - bug
  areas:
  - test
