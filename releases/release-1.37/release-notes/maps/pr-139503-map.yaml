pr: 139503
pr_body: ''
releasenote:
  text: Fixed a `kube-proxy` issue on Windows where transient HNS downtime during
    restart or recovery could cause incorrect LoadBalancer state reconciliation, resulting
    in duplicate LoadBalancer creation failures with `0xb7` errors.
  sigs:
  - network
  - windows
  kinds:
  - bug
  areas:
  - kube-proxy
